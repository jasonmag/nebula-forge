<article class="flex max-w-xl flex-col items-start justify-between" id="<%= dom_id blog %>">
  <div class="flex items-center gap-x-4 text-xs">
    <!-- <time datetime="2020-03-16" class="text-gray-500"><%= blog.created_at.strftime("%B %d, %Y") %></time> -->
    <%= render partial: 'home/shared/tags', locals: { tags: blog.tags } %>
  </div>
  <!-- content -->
  <div class="group relative">
    <h3 class="mt-3 text-lg font-semibold leading-6 text-gray-900 group-hover:text-gray-600">
      <%= link_to blog.title, blog, class: "absolute inset-0" %>
    </h3>
    <p class="mt-14 line-clamp-4 text-sm leading-6 text-gray-600 trix-content"><%= truncate_body_for_preview(blog.content) %></p>
  </div>
  <!-- author -->
  <%= render partial:'home/shared/author', locals: { author: blog.author } %>
</article>